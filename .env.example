# Root environment example for docker-compose
# Copy this file to `.env` and update values before running `docker compose up`.

# Backend service environment
FLASK_ENV=production
PORT=5000
# If you use firebase admin, mount credentials in the backend container and set this path
# FIREBASE_CREDENTIALS=/run/secrets/firebase_credentials.json

# Database / external services
# MONGO_URI=mongodb://user:pass@mongo:27017/dbname

# Any other secrets (do NOT commit real values)
# SECRET_KEY=replace-with-a-secret
