# Example environment variables for backend
# Copy to .env and fill values before running locally or in production.

# Flask settings
FLASK_ENV=production
PORT=5000

# Firebase / service account path (optional)
# FIREBASE_CREDENTIALS=/path/to/serviceAccountKey.json

# Database URL (if using Mongo / Redis)
# MONGO_URI=mongodb://user:pass@mongo:27017/dbname

# Any other secrets (do NOT commit real secrets into repo)
# SECRET_KEY=replace-with-a-random-secret
